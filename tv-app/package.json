{
  "name": "tv-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Thin broadcast channel client - pairs once, then plays the live channel forever",
  "scripts": {
    "dev": "vite dev --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "android:sync": "npm run build && npx cap sync android",
    "android:apk": "npm run android:sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@lightningjs/blits": "^2.8.9",
    "hls.js": "^1.7.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "@lightningjs/msdf-generator": "^1.3.0",
    "vite": "^5.4.8"
  }
}
